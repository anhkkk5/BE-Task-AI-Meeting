# Projects Module

NV5 quan ly project trong workspace.

- Project luon duoc truy van theo `workspaceId` va `projectId`.
- `createdBy` lay tu JWT user hien tai.
- `workspaceId` lay tu route params.
- `keyCode` unique trong tung workspace.
- Archive/complete chi doi `status`, khong hard delete.
